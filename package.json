{
  "name": "pitch-detector",
  "version": "1.0.0",
  "description": "Real-time pitch detection and tuner application with YIN and Pitchy algorithms",
  "main": "script.js",
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.js",
    "test:e2e": "CI=1 playwright test",
    "test:e2e:headed": "CI=1 playwright test --headed",
    "serve": "serve -n",
    "generate-icons": "node scripts/generate-icons.js",
    "start": "npm run serve"
  },
  "keywords": [
    "pitch-detection",
    "tuner",
    "yin-algorithm",
    "autocorrelation",
    "music",
    "audio-processing"
  ],
  "author": "cho45",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@resvg/resvg-js": "^2.6.2",
    "serve": "^14.2.5"
  }
}
